rootProject.name = 'mz-study-msa'

include 'boot'
include 'presentation'
include 'application'
include 'domain'
include 'infrastructure'

def subProjectDir = "subprojects";
for (project in rootProject.children) {
    project.projectDir = file("${subProjectDir}/${project.name}")
}
